import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.lancergroup.lvargas9715',
  appName: 'AppLancerGroup',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
